/*

  - All length default values ​​are 10
  - For the string data type, you can customize the result.
  - Example

  randomres.String({
    length: 15,
    charset: 'alphabet
  })

  Charset values ​​are currently alphabet only

*/

module.exports = require('./src/String')